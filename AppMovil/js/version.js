// Version del bundle web. El CI la reemplaza en cada build (formato 1.0.N).
// En desarrollo local queda como 'dev'. La usa updater.js para decidir si hay
// una actualizacion OTA mas reciente que la que se esta ejecutando.
window.__APP_BUNDLE_VERSION__ = 'dev';
